# This is a basic workflow to help you get started with Actions

name: Build Cloud Agent
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
        with :
          repository: bosch/swdc-os-cloud-agent
          path: cloudagent
          token: ${{ secrets.BUILD_CLOUD_AGENT }}
          ref: 'add_build_actions'

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
            go-version: 1.17
        
      # Runs a single command using the runners shell
      - name: Run build script
        run: | 
          cd $GITHUB_WORKSPACE/cloudagent/cmd/azure
          ./build.sh

      # Runs a set of commands using the runners shell
      - name: Check build
        run: |
          cd $GITHUB_WORKSPACE/cloudagent/cmd/azure/target/natives/linux-arm64
          ls -l
          cp -rf cloudagent /home/runner/work
          
      - uses: actions/checkout@v2
      - run: |
          cd .github
          cp -rf /home/runner/work/cloudagent cloudagent
          date > generated.txt
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .
          git add /home/runner/work/cloudagent
          git commit -m "generated"
          git push

